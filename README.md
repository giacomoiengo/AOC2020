# AOC2020
Let's see which day I can reach this year.

In order to test a day just type: `$ python3 solve.py input.txt`  
  
If you get wrong result from `solve.py` remember that `input.txt` must end without empty lines.

Since I would like to finish the whole challenge first, some problem may not have a well written solution, but i will try to produce a more elegant one at a later time.
